const ProductCartReducer = (state, action) => {
    switch (action.type) {
        case value:
            return {
                
            }
    
        default:
            return state;
    }
}